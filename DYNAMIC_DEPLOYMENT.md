# 动态版部署建议

## 结论

可以升级。GitHub Pages 仍可托管前端，但它不能安全保存服务器密钥，也不能直接提供后端接口。动态版需要额外的身份认证、数据库和私有文件存储。

## 推荐架构

- 前端：继续使用当前 HTML/CSS/JavaScript，部署到 Vercel、Cloudflare Pages，或保留 GitHub Pages。
- 登录与数据库：Supabase Auth + Postgres。
- 成绩截图：Supabase Storage 私有 bucket，使用登录用户的短期签名链接查看。
- 权限：所有业务表都带 `user_id`，并启用 Row Level Security（RLS）。
- 服务端任务：只有确实需要服务器私钥的操作才放到 Vercel Functions、Cloudflare Workers 或 Supabase Edge Functions。

## 上线前要求

1. 确定数据主体：每位教师只能看到自己的学生；如需教务协作，再增加机构和角色模型。
2. 建立数据表：学生、试听、课程、成绩、材料记录和文件元数据。
3. 启用账号登录，并为每张表编写、测试 RLS 规则。
4. 创建私有截图存储，限制文件类型、大小和访问期限。
5. 前端只使用 Supabase publishable/anon key；`service_role`、数据库密码等只能放在服务端环境变量里。
6. 准备隐私告知、数据删除、备份恢复和账号注销流程。
7. 使用两个测试账号验证互相看不到对方的数据。

## 部署步骤

1. 在 Supabase 创建项目，建立表、索引、外键、RLS 和私有 Storage bucket。
2. 在本地使用测试项目连接，完成注册、登录、增删改查、上传截图和退出登录测试。
3. 在前端托管平台连接 GitHub 仓库，配置公开的项目 URL 和 publishable/anon key。
4. 将服务器专用密钥只配置在 Functions/Workers 的加密环境变量中，不写入仓库和前端构建产物。
5. 设置正式域名和 Supabase Auth 的允许回调地址。
6. 部署预览环境，完成多账号隔离、移动端、断网、重复提交和文件上传测试。
7. 将旧浏览器数据通过一次性导入工具迁移到登录账号；确认无误后停止使用本地旧数据。
8. 发布正式环境，并持续检查错误日志、备份和异常登录。

## 是否需要购买

作品集和少量测试用户通常可以先使用各平台的免费额度。正式使用后是否付费取决于数据库容量、文件存储、流量、备份和并发量。自定义域名不是必须；需要品牌化网址时再购买即可。

## 当前项目最合适的下一步

先保留静态版作为公开作品集演示，所有示例数据保持虚构；另建一个受登录保护的动态预览环境。这样 HR 能直接打开作品，而真实学生数据只进入有权限控制的系统。
